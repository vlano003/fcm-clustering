apiVersion: batch/v1
kind: Job
metadata:
  name: fcm-job
spec:
  template:
    metadata:
      labels:
        app: fcm
    spec:
      containers:
      - name: fcm
        image: ghcr.io/vlano003/fcm-clustering:latest
        # Якщо потрібно зберегти графік, монтуйте volume в /app/output
      restartPolicy: OnFailure